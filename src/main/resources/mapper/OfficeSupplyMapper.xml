<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.bunsanedthinking_springback.repository.OfficeSupplyMapper">
    <insert id="insert_OfficeSupply" parameterType="com.example.bunsanedthinking_springback.vo.OfficeSupplyVO">
        INSERT INTO officesupply (id, name, description, inventory, total_inventory, department_id)
        VALUES (#{id},
                #{name},
                #{description},
                #{inventory},
                #{total_inventory},
                #{department_id})
    </insert>
    <delete id="delete_OfficeSupply" parameterType="int">
        DELETE FROM officesupply
        WHERE id = #{id}
    </delete>
    <select id="findById_OfficeSupply" parameterType="int" resultType="com.example.bunsanedthinking_springback.vo.OfficeSupplyVO">
        SELECT * FROM officesupply
        WHERE id = #{id}
    </select>
    <select id="findByName_OfficeSupply" parameterType="String" resultType="com.example.bunsanedthinking_springback.vo.OfficeSupplyVO">
        SELECT * FROM officesupply
        WHERE name = #{name}
    </select>
    <update id="update_OfficeSupply" parameterType="com.example.bunsanedthinking_springback.vo.OfficeSupplyVO">
        UPDATE officesupply
        SET name = #{name},
            description = #{description},
            inventory = #{inventory}
        WHERE id = #{id}
    </update>
    <select id="getAll_OfficeSupply" resultType="com.example.bunsanedthinking_springback.vo.OfficeSupplyVO">
        SELECT * FROM officesupply
    </select>
    <select id="getTotalInventory_OfficeSupply" resultType="int">
        SELECT SUM(inventory) FROM officesupply
    </select>
    <select id="getMaxId_Administrative" resultType="Integer">
        SELECT MAX(id) FROM officesupply
    </select>
    <update id="update" parameterType="com.example.bunsanedthinking_springback.vo.OfficeSupplyVO">
        UPDATE officesupply
        SET
            inventory = #{inventory},
            name = #{name},
            total_inventory = #{total_inventory},
            description = #{description},
            department_id = #{department_id}
        WHERE id = #{id}
    </update>
</mapper>