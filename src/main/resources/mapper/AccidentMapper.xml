<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.bunsanedthinking_springback.repository.AccidentMapper">
    <select id="getAccidentById_Customer" resultType="com.example.bunsanedthinking_springback.vo.AccidentVO">
        select * from accident
        where id = #{id}
    </select>
    <select id="getAllByCustomerId_Customer" resultType="com.example.bunsanedthinking_springback.vo.AccidentVO">
        select * from accident
        where customer_id = #{id}
    </select>
    <select id="getAccidentById_Compensation" resultType="com.example.bunsanedthinking_springback.vo.AccidentVO">
        select * from accident
        where id = #{id}
    </select>
</mapper>