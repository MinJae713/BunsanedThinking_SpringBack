<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.bunsanedthinking_springback.repository.PaymentDetailMapper">
    <select id="getCount_Compensation" resultType="int">
        select count(*) from paymentdetail
    </select>
    <select id="getLastId_Compensation" resultType="int">
        select max(id) from paymentdetail
    </select>
    <insert id="add_Compensation" parameterType="com.example.bunsanedthinking_springback.vo.PaymentDetailVO">
        insert into paymentdetail (id, account_holder, bank, bank_account, money, payment_type, process_status, contract_id, employee_id)
        VALUES (#{id}, #{account_holder}, #{bank}, #{bank_account}, #{money}, #{payment_type}, #{process_status}, #{contract_id}, #{employee_id})
    </insert>
</mapper>